meta {
  name: Bento (List)
  type: http
  seq: 17
}

get {
  url: {{base_url}}/api/v1/bentos
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
  {
    "name": "My Bento"
  }
}
